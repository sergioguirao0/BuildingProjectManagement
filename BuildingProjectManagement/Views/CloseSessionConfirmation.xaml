<Window x:Class="BuildingProjectManagement.Views.CloseSessionConfirmation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BuildingProjectManagement.Views"
        mc:Ignorable="d"
        Title="CloseSessionConfirmation" Height="200" Width="350" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" MouseDown="Window_MouseDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Dictionaries/WindowStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border CornerRadius="10" BorderThickness="5" Style="{StaticResource BorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="5"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Cerrar sesión" Style="{StaticResource topBarText}"/>
                <Button x:Name="btClose" Grid.Column="1" Background="#FF2B2B2B" Content="X" 
                    Style="{StaticResource TopBarButton}" Click="btClose_Click"/>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Se cerrará la sesión actual" HorizontalAlignment="Center"
                           VerticalAlignment="Center" FontSize="14"/>
                    <TextBlock Text="¿Desea continuar?" HorizontalAlignment="Center" VerticalAlignment="Center" 
                           FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Margin="0,-15,0,0">
                    <Button x:Name="btAccept" Style="{StaticResource ControlButton}" Content="ACEPTAR"
                        Click="btAccept_Click" Cursor="Hand"/>
                    <Button x:Name="btCancel" Style="{StaticResource ControlButton}" Content="CANCELAR"
                        Margin="10,0,0,0" Click="btCancel_Click" Cursor="Hand" Background="#FF585858"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
